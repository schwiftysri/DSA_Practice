def window_skeleton(s:str):
    left,right = 0,0
    while right<len(s):
        c = s[right]
        right +=1
        while wwindow_nees_shrink:
            d = s[left]
            left+=1

def window_skeleton(s:str):
    left,right = 0,0
    while right< len(s):
        c = s[right]
        right+=1
        while window_needs_shrink:
            d = s[left]
            left+=1


def window_skeleton(s:str):
    left,right = 0,0
    while right<len(s):
        c = s[right]
        right+=1
        while window_needs_shrink:
            d=s[left]
            left+=1

def window_skeleton(s:str):
    left,right = 0,0
    while right<len(s):
        c = s[right]
        right+=1
        while window_needs_shrink:
            d = s[left]
            left+=1


def window_skeleton(s:str):
    left,right = 0,0
    while right<len(s):
        c = s[right]
        right+=1
        while window_needs_shrink:
            d = s[left]
            left+=1

def window_skeleton(s:str):
    left,right = 0,0
    while right<len(s)
        c = s[right]
        right+=1
        while window_needs_shrink:
            d = s[left]
            left+=1

def window_skeleton(s:str):
    left,right = 0,0
    while right<len(s):
        c = s[right]
        right+=1
        while window_needs_shrink:
            d = s[left]
            left+=1

def window_skeleton(s:str):
    left, right = 0,0
    while right< len(s):
        c = s[right]
        right+=1
        while window_needs_shrink:
            d = s[left]
            left+=1

def window_skeleton(s:str):
    left , right = 0,0
    while right<len(s):
        c = s[right]
        right+=1
        while window_needs_shrink:
            d = s[left]
            left+=1

def window_skeleton(s:str):
    left,right = 0,0
    while right< len(s):
        c = s[right]
        right+=1
        while window_needs_shrink:
            d = s[left]
            left+=1

def window_skeleton(s:str):
    left,right = 0,0
    while right< len(s):
        c = s[right]
        right+=1
        while window_needs_shirnk:
            d = s[left]
            left+=1

def window_skeleton(s:str):
    left,right = 0,0
    while right<len(s):
        c = s[right]
        right+=1
        while window_needs_shrink:
            d = s[left]
            left+=1

def window_skeleton(s:str):
    left,right = 0,0
    while right<len(s):
        c = s[right]
        right+=1
        while window_needs_shrink:
            d = s[left]
            left+=1

def window_skeleton(s:str):
    left,right = 0,0
    while right<len(s):
        c = s[right]
        right+=1
        while window_needs_shrink:
            d = s[left]
            left+=1

def window_skeleton(s:str):
    left,right = 0,0
    while right<len(s):
        c = s[right]
        right+=1
        while window_needs_shrink:
            d = s[left]
            left+=1

    
def window_skeleton(s:str):
    left,right = 0,0
    while right< len(s):
        c = s[right]
        right+=1
        while window_needs_shrink:
            d = s[left]
            left+=1

def window_skeleton(s:str):
    left,right = 0,0
    while right<len(s):
        c = s[right]
        right+=1
        while window_needs_shrink:
            d = s[left]
            right+=1

def window_skeleton(s:str):
    left,right = 0,0
    while right<len(s):
        c = s[right]
        right+=1
        while window_needs_shrink:
            d = s[left]
            right+=1

def window_skeleton(s:str):
    left,right = 0,0
    while right<len(s):
        c = s[right]
        right+=1
        while window_needs_shrink:
            d = s[left]
            left+=1

def window_skeleton(s:str):
    left,right = 0,0
    while right<len(s):
        c = s[right]
        right+=1
        while window_needs_shrink:
            d = s[left]
            left+=1

def window_skeleton(s:str):
    left,right = 0,0
    while right<len(s):
        c = s[right]
        right+=1
        while window_needs_shrink:
            d = s[left]
            left+=1

def window_skeleton(s:str):
    left,right = 0,0
    while right<len(s):
        c = s[right]
        right+=1
        while window_needs_shrink:
            d = s[left]
            left+=1

def window_skeleton(s:str):
    left, right = 0,0
    while right<len(s):
        c = s[right]
        right+=1
        while window_needs_shrink:
            d = s[left]
            left+=1


def min_window(s:str, t:str):
    need = {}
    for c in t:
        need[c] = need.get(c,0) +1
    window = {}
    valid = 0
    left,right = 0,0
    while right<len(s):
        c = s[right]
        right+=1

        if c in need:
            window[c] = window.get(c,0) +1
            if window[c] == need[c]:
                valid+=1
        while window_needs_shrink:
            d = s[left]
            left+=1

def min_window(s:str, t:str):
    need = {}
    for c in t:
        need[c] = need.get(c,0)+1
    window = {}
    valid=0
    while right< len(s):
        c = s[right]
        right+=1

        if c in t:
            window[c] = window.get(c,0)+1
            if window[c] == need[c]:
                valid+=1
        while window_needs_shrink:
            d = s[left]
            left+=1

def min_window(s:str, t:str):
    need,window = {}, {}
    for c in t:
        need[c] = need.get(c,0) +1
    window= {}
    valid = 0
    while right< len(s):
        c = s[right]
        right+=1
        if c in t:
            window[c] = window.get(c,0) +1
            if window[c] == need[c]:
                valid+=1
        while window_needs_shrink:
            d = s[left]
            left+=1

def min_window(s:str, t:str):
    need, window = {}, {}
    for c in t:
        need[c] = need.get(c,0)+1
    left,right = 0,0
    valid = 0
    while right<len(s):
        c = s[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            valid+=1
        while window_needs_shrink:
            d = s[left]
            left+=1

def min_window(s:str, t:str):
    need, window = {},{}
    for c in t:
        need[c] = need.get(c,0)+1
    left,right,valid = 0,0,0

    while right< len(s):
        c = s[right]
        right+=1
        if c in need:
            window[c] = window(c,0)+1
            if window[c] == need[c]:
                valid+=1
        while window_needs_shrink:
            d = s[left]
            left+=1

def min_window(s:str, t:str):
    need,window = {},{}
    left,right,valid = 0,0,0
    for c in t:
        need[c] = need.get(c,0)+1
    while right<len(s):
        c = s[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] == need[c]:
                valid+=1
        while window_needs_shrink:
            d = s[left]
            left+=1

def min_window(s:str, t:str):
    need,window= {},{}
    for c in t:
        need[c] = need.get(c,0)+1
    left, right,valid = 0,0,0
    while right<len(s):
        c = s[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] ==need[c]:
                valid+=1
        while window_needs_shrink:
            d = s[left]
            left+=1

def min_window(s:str, t:str):
    need,window= {},{}
    left,right,valid = 0,0,0
    for c in t:
        need[c] = need.get(c,0)+1
    while right< len(s):
        c = s[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] == need[c]:
                valid+=1
        while window_needs_shrink:
            d = s[left]
            left+=1

def min_window(s:str, t:str):
    need,window = {},{}
    left,right,valid = 0,0,0
    for c in t:
        need[c] = need.get(c,0)+1
    while right< len(s):
        c = s[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] == need[c]:
                valid+=1
        while valid == len(need):
            d = s[left]
            left+=1
            if d in need:
                window[d]-=1
                if window[d]<need[d]:
                    valid-=1


def min_window(s:str, t:str):
    need,window = {},{}
    left,right,valid = 0,0,0
    for c in t:
        need[c] = need.get(c,0)+1
    while right<len(s):
        c = s[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] == need[c]:
                valid+=1
        while valid ==len(need):
            d = s[left]
            left+=1
            if d in need:
                if window[d] == need[d]:
                    valid-=1
                window[d] -=1

def min_window(s:str, t:str):
    need,window = {},{}
    left,right,valid = 0,0,0
    for c in t:
        need[c] = need.get(c,0)+1
    while right<len(s):
        c = s[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] ==need[c]:
                valid+=1
        while valid == len(need):
            d = s[left]
            left+=1
            if d in need:
                if window[d] == need[d]:
                    valid-=1
                window[d]-=1

def min_window(s:str, t:str):
    need,window = {},{}
    left,right,valid = 0,0,0
    for c in t:
        need[c] = need.get(c,0)+1
    while right<len(s):
        c = s[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] ==need[c]:
                valid+=1
        while valid==len(need):
            d = s[left]
            left-=1
            if d in need:
                if window[d] ==need[d]:
                    valid-=1
                window[d]-=1

def min_window(s:str, t:str):
    need,window = {}, {}
    left,right,valid = 0,0,0
    for c in t:
        need[c] = need.get(c,0)+1
    while right<len(s):
        c = s[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] == need[c]:
                valid+=1
        while valid == len(need):
            d = s[left]
            left+=1
            if d in need:
                if window[d] == need[d]:
                    valid-=1
                window[d]-=1

def min_window(s:str, t:str):
    need, window = {},{}
    left,right,valid = 0,0,0
    for c in t:
        need[c] = need.get(c,0)+1
    while right<len(s):
        c = s[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] == need[c]:
                valid+=1
        while valid == len(need):
            d = s[left]
            left+=1
            if d in need:
                if window[d] ==need[d]:
                    valid-=1
                window[d] -=1


def min_window(s:str, t:str):
    need, window = {},{}
    left, right, valid = 0,0,0
    for c in t:
        need[c] = need.get(c,0)+1
    while right<len(s):
        c = s[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] == need[c]:
                valid+=1
        while valid== len(need):
            d = s[left]
            left+=1
            if d in need:
                if window[d] == need[d]:
                    valid-=1
                window[d]-=1
                
def min_window(s:str, t:str):
    need,window = {},{}
    left, right = 0,0,0
    start , length = 0, float('inf')
    for c in t:
        need[c] = need.get(c,0)+1
    while right<len(s):
        c = s[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] == need[c]:
                valid+=1
        while valid == len(need):
            if right-left< length:
                start = left
                length = right - left
                d = s[left]
                left+=1
                if d in need:
                    if window[d] == need[d]:
                        valid-=1
                    window[d] -=1
    return "" if length == float('inf') else s[start: start+length]

def min_window(s:str, t:str):
    need = {}
    window = {}
    start,left,right,valid = 0,0,0,0
    length = float('inf')
    for c in t:
        need[c] = need.get(c,0)+1
    while right< len(s):
        c = s[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] == need[c]:
                valid +=1
        while valid == len(need):
            if right - left< length:
                start = left
                length = right - left
            d = s[left]
            left+=1
            if d in need:
                if window[d] == need[d]:
                    valid -=1
                window[d] -=1
    return "" if length == float('inf') else s[start:start+length]


def min_window(s:str, t:str):
    need,window = {},{}
    left,right,start,valid, length = 0,0,0,0,float('inf')
    for c in t:
        need[c] = need.get(c,0)+1
    while right<len(s):
        c = s[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] == need[c]:
                valid +=1
        while valid == len(need):
            if right - left < length:
                start = left
                length = right - left
            d = s[left]
            left+=1
            if d in need:
                if window[d] == need[d]:
                    valid-=1
                window[d] -=1
    return "" if length = float('inf') else s[start:start+length]


def min_window(s:str, t:str):
    need,window = {},{}
    left,right,valid,start,length = 0,0,0,0,float('inf')
    for c in t:
        need[c] = need.get(c,0)+1
    while right<len(s):
        c = s[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] ==need[c]:
                valid+=1
        while valid == len(need):
            if right-left<length:
                start = left
                length = right- left
            d = s[left]
            left+=1
            if d in need:
                if window[d] == need[d]:
                    valid-=1
                window[d]-=1
    return "" if length == float('inf') else s[start:start+length]

def min_window(s:str, t:str):
    need,window = {},{}
    left,right,start,valid,length = 0,0,0,0,float('inf')
    for c in t:
        need[c] = need.get(c,0)
    while right<len(s):
        c = s[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] == need[c]:
                valid +=1
        while valid == len(need):
            if right - left < length:
                start = left
                length = right - left
            d = s[left]
            left+=1
            if d in need:
                if window[d] == need[d]:
                    valid -=1
                window[d]-=1
    return "" if length == float('inf') else s[start:start+length]

def min_window(s:str, t:str):
    need,window = {},{}
    left,right,valid,start,length = 0,0,0,0,float('inf')
    for c in t:
        need[c] = need.get(c,0)+1
    while right<len(s):
        c = s[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] == need[c]:
                valid +=1
        while valid == len(need):
            if right - left< length:
                start = left
                length = right - left
            d = s[left]
            left+=1
            if d in need:
                if window[d] == need[d]:
                    valid -=1
                window[d] -=1
    return "" if length == float('inf') else s[start:start+length]

def min_window(s:str, t:str):
    need,window = {},{}
    left,right,start,valid,length = 0,0,0,0,float('inf')
    for c in t:
        need[c] = need.get(c,0)+1
    while right<len(s):
        c = s[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] == need[c]:
                valid +=1
        while valid == len(need):
            if right - left < length:
                start = left
                length = right - left
            d = s[left]
            left+=1
            if d in need:
                if window[d] == need[d]:
                    valid-=1
                window[d] -=1
    return "" if length == float('inf') else s[start:start+length]

def min_window(s:str, t:str):
    need,window={},{}
    left,right,valid,start,length = 0,0,0,0,float('inf')
    for c in t:
        need[c] = need.get(c,0)+1
    while right<len(s):
        c = s[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] == need[c]:
                valid +=1
        while valid == len(need):
            if right - left <length:
                start = left
                length = right - left
            d = s[left]
            left+=1
            if d in need:
                if window[d] == need[d]:
                    valid -=1
                window[d] -=1
    return "" if length = float('inf') else s[start:start+lenght]


def min_window(s:str, t:str):
    need,window = {},{}
    left,right,start,valid,length = 0,0,0,0,float('inf')
    for c in t:
        need[c] = need.get(c,0)+1
    while right<len(s):
        c = s[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] == need[c]:
                valid +=1
        while valid == len(need):
            if right - left <length:
                start = left
                length = right -left
            d = s[left]
            left+=1
            if d in need:
                if window[d] == need[d]:
                    valid -=1
                window[d]-=1

    return "" if length == float('inf') else s[start:start+length]

def min_window(s:str, t:str):
    need,window = {},{}
    left,right,valid , start,length = 0,0,0,0,float('inf')
    for c in t:
        need[c] = need.get(c,0)+1
    while right<len(s):
        c = s[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] == need[c]:
                valid +=1
        while valid == len(need):
            if right - left < length:
                start = left
                length = right - left
            d = s[left]
            left+=1
            if d in need:
                if window[d] == need[d]:
                    valid -=1
                window[d]-=1
    return "" if length == float('inf') else s[start:start+length]

def min_window(s:str, t:str):
    need,window = {},{}
    left,right,valid,start,length = 0,0,0,0,float('inf')
    for c in t:
        need[c] = need.get(c,0)+1
    while right<len(s):
        c = s[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] == need[c]:
                valid +=1
        while valid == len(need):
            if right - left< length:
                start = left
                length = right - left
            d = s[left]
            left+=1
            if d in need:
                if window[d] == need[d]:
                    valid -=1
                window[d]-=1
    return "" if length == float('inf') else s[start:start+length]


def longest_substring(s:str):
    window = {}

    while right<len(s):
        window = {}
        left,right,start,length = 0,0,0,float('inf')
        c = s[right]
        right+=1
        window[c] = window.get(c,0)+1
        while window.get(c,0)>1:
            if right - left< length:
                length = right -left
                start = left
            d = s[left]
            left+=1
            window[d]-=1
    return "" if length == float('inf') else s[start:start+length]

def longest_subsrting(s:str):
    window = {}
    left,right,start,length = 0,0,0,0
    while right<len(s):
        c = s[right]
        right+=1
        window[c] = window.get(c,0)+1
        while window[c]>1:
            d = s[left]
            left+=1
            window[d]-=1
        if right - left>length:
            length = right - left
            start = left
    return length


def checkInclusion(s1:str, s2:str):
    window, need = {},{}
    left ,right = 0,0
    valid = 0
    for c in s2:
        need[c] = need.get(c,0)+1

    while right<len(s1):
        c = s1[right]
        right+=1
        while c in need:
            window[c] = window.get(c,0)+1
            if window[c] == need[c]:
                valid+=1
        if valid == len(need):
            return True
        left = right
        window = {}
    return False

def checkInclusion(s1:str, s2:str):
    window, need = {},{}
    left, right = 0,0

    for c in s1:
        need[c] = need.get(c,0)+1
    while right<len(s2):
        c = s2[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] == need[c]:
                valid +=1
        while right - left>len(s1):

            d = s2[left]
            left+=1
            if d in need:
                if window[d] == need[d]:
                    valid -=1
                window[d]-=1
        if valid == len(need):
            return True
    return False


def checkInclusion(s1:str,s2:str):
    window,need = {},{}
    left,right,valid = 0,0,0
    for c in s1:
        need[c] = need.get(c,0)+1
    while right<len(s2):
        c = s2[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] == need[c]:
                valid +=1
        while right - left>len(s1):
            d = s2[left]
            left+=1
            if d in need:
                if window[d] == need[d]:
                    valid -=1
                window[d]-=1
        if valid == len(need):
            return True
    return False

def checkInclusion(s1:str, s2:str):
    window,need = {},{}
    left,right,valid = 0,0,0
    for c in s1:
        need[c] = need.get(c,0)+1
    while right<len(s2):
        c = s2[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window = window.get(c,0)+1:
                valid +=1
        while right- left <len(s1):
            d = s[left]
            left+=1
            if d in need:
                if window[d] == need[d]:
                    valid -=1
                window[d] -=1
        if valid == len(need):
            return True
    return False

def checkInclusion(s1:str, s2:str):
    need , window = {},{}
    left,right,valid = 0,0,0
    for c in s1:
        need = need.get(c,0)+1
    while right<len(s2):
        c = s[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] == need[c]:
                valid +=1
        while right - left > len(s1):
            d = s2[left]
            left+=1
            if d in need:
                if window[d] == need[d]:
                    valid -=1
                window[d] -=1
        if valid == len(need):
            return True
    return False


def checkInclusion(s1:str, s2:str):
    need,window = {},{}
    left,right,valid = 0,0,0
    for c in s1:
        need[c] = need.get(c,0)+1
    while right<len(s2):
        c = s2[right]
        right +=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] == need[]:
                valid+=1
        while right - left >len(s1):
            d = s2[left]
            left+=1
            if d in need:
                if window[d] == need[d]:
                    valid -=1
                window[d] -=1
        if valid == len(need):
            return True
    return False


def checkInclusion(s1:str,s2:str):
    need,window = {},{}
    left,right,valid = 0,0,0
    for c in s1:
         need[c] = need.get(c,0)+1
    while right<len(s2):
        c = s2[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] == need[c]:
                valid+=1
        while right-left>len(s1):
            d = s2[left]
            left+=1
            if d in need:
                if window[d] == need[d]:
                    valid-=1
                window[d]-=1
        if valid == len(need):
            return True
    return False

def checkInclusion(s1:str, s2:str):
    need = {}
    window = {}
    left,right = 0,0
    valid = 0
    for c in s1:
        need[c] = need.get(c,0)+1

    while right<len(s2):
        c = s2[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] == need[c]:
                valid+=1
        while right - left > len(s1):
            d = s2[left]
            left+=1
            if d in need:
                if window[d] == need[d]:
                    valid -=1
                window[d] -=1
        if valid == len(need):
            return True
    return False

def findAnagrams(s1:str,s2:str) -> list([int]):
    need, window = {},{}
    left,right,valid = 0,0,0
    res = []
    for c in s1:
        need[c] = need.get(c,0)+1
    while right<len(s2):
        c = s2[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] == need[c]:
                valid +=1
        while right - left>len(s1):
            d = s2[left]
            left+=1
            if d in need:
                if window[d] == need[d]:
                    valid -=1
                window[d] -=1
        if valid == len(need):
            res.append(left)
    return res

def finAnagrams(s:str, p:str)-> list([int]):
    need, window = {}
    left, right, valid = 0,0,0
    res = []
    for c in p:
        need[c] = need.get(c,0)+1
    while right<len(s):
        c = s[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] == need[c]:
                valid +=1
        while right - left > len(p):
            d = s[left]
            left +=1
            if d in need:
                if window[d] == need[d]:
                    valid -=1
                window[d] -=1
        if valid == len(need):
            res.append(left)
    return res

def findAnagrams(s:str, p:str) -> list([int]):
    need, window = {},{}
    left,right,valid = 0,0,0
    res = []
    for c in p:
        need[c] = need.get(c,0)+1
    while right<len(s):
        c = s[right]
        right+=1
        if c in need:
            window[c] = window.get(c,0)+1
            if window[c] == need[c]:
                valid +=1
        while right - left > len(p):
            d = s[left]
            left+=1
            if d in need:
                if window[d] == need[d]:
                    valid -=1
                window[d] -=1
        if valid ==len(need):
            res.append(left)
    return res